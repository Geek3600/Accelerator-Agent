# OPT-125M Hardware Accelerator

## 背景
本项目是一个FPGA流水线硬件加速器，用于加速 OPT-125M 模型的推理，主要是两个场景，一个是序列长度26，另一个是序列长度912，目前处于功能验证阶段，时间非常紧张，需要尽全力修复bug。

## 目标
- 本加速器是流水线加速器，一共11级流水线，权重和数据都是双缓冲，用于计算与访存重叠，一层计算时需要并行的读取下一层的权重，实现流水线式的运行。
- 对加速器代码进行全序列pipeline功能验证，要完全跑通912序列pipeline的功能验证，功能验证完成后要使用远端服务器进行综合，布局布线，最后烧写位流。
- 需要验证并最终实现一个功能正确的，能在FPGA上运行的硬件加速器，最终项目验收时需要在VU9P fpga板卡上进行上板运行。
- 最终加速器能支持序列长度26和912的opt125M 12层模型的推理，目前不要求加速器输出结果与原始模型的输出数值完全一致，目前只需要能上板跑通12层的流水线式的推理，不卡死，有有效数据输出即可
- 能完整运行opt125M 12层模型的流水线推理，包括序列长度26和912的推理，

## 相关文档
- 上下文文档记录：/home/remote/workspace/Qwen2-Accelerator/docs/context_2026-03-27.md，详细上下文文档中的最后几节是最新内容，按时间顺序排列，精确到时分秒，用于恢复上下文
- 硬件加速器前期设计文档
  - 流水线架构设计文档：/home/remote/workspace/Qwen2-Accelerator/docs/gptarc2.pdf
  - 量化方案文档：/home/remote/workspace/Qwen2-Accelerator/docs/opt_quan.md

## 规则 / 约束
- 代码使用最小实现，最小修改
- 代码保持原有代码的风格，保证代码的可读性
- 在功能验证时仔细分析问题，找出根因，及时修复。
- 每次有新进展，需要及时更新到上下文文档，防止由于会话中断导致上下文丢失
- 使用远端服务器做vcs编译和验证
- 功能验证时必须使用真实数据和权重进行验证，保证输出数据的规模与真实输出一致
- 每次在远端服务器运行时，可将不需要再使用的，旧的进程终止，保证验证环境干净清洁；并确保已经将新内容同步到远端，防止由于重复运行旧文件造成的时间浪费和bug误判
- 每次bug解决时，都需要说明具体是做了什么改动才解决的问题，保证我对于项目代码细节的知情

## 加速器硬件功能验证规则 / 约束
- 在远端服务器上的vcs进行功能验证，在本地进行硬件代码的修改，修改之后将代码同步到远端服务器上进行验证。
- 功能验证时，必须使用真实数据和权重数据进行验证，保证输出数据的规模与真实输出一致，目前我们的验证目标是整个12层模型的流水线推理能上板与逆行，并且不卡死，有有效数据输出即可，不要求输出数据与原始模型的输出数值完全一致。
- 功能验证时，必须使用真实工程中的接口或者模型进行功能验证，保证与真实上板时的DDR AXI接口保持一致，包括数据的读写，权重的读写，以及模型的推理。
- 功能验证时，必须保证是opt125M模型的12层，11级流水线保证流水线式的模型推理运行，双缓冲机制，这些机制必须保证不能做任何简化。
- 必须严格保证功能验证时的运行流程与真实上板跑的流程一致！这样尽可能减少后续上板所需要的调试时间。
- 请注意修改时不要添加过多的资源，因为现在的资源使用量和布局布线已经很拥塞了
- 功能验证一定要保证进行的是真正流水线式的运行，也就是比如：tokenN完成第一级流水线并进入第二级流水线之后，tokenN+1马上开始第一级流水线，而不是等待tokenN完成全部流水线后才开始下一个token
  
## Task Priority Rules
- The current user prompt always has higher priority than `docs/context_2026-03-27.md`.
- CONTEXT.md is for recovery only, not for overriding the current task.
- Do not continue the previous task unless the user explicitly asks to continue it.
- When the user starts a new task, first restate the new task in one sentence and execute only that task.

## 远程服务器
- 远程服务器的IP地址：hyyuan@10.12.133.23
- 已经设置好免密登录
- fpga样例工程：/home/hyyuan/workspace/v6.0_9p_cnn_2slr_4core_yolov8

## Spatial Accelerator LLM Multi-Agent自动设计系统的开发约束
* 背景：我们研究和开发的是一个能够自主设计fpga LLM 空间加速器的multi-agent系统，目的不是为了提出一种通用的multi-agent机制，而是设计了一个针对fpga LLM 空间加速器的领域专家式 multi-agent 自动设计工作流。
* 重点目标：本课题的目标是DAC2027最佳论文，是科学研究，不单纯是工程实现
* 既然是agent系统，我们就必须接入LLM，LLM作为agent系统的大脑，负责理解自然语言的输入、各种非结构化和结构化的输入信息、工具调用的结果等等，并将这些零散的，细碎的信息提炼、总结和转换成下一步的行动计划。
* 不要仅仅使用smoke test测试通过就认为功能已经实现好，必须要跑通真实应用场景下的真实流程，且中间不能做任何的简化，才能算是功能实现完成。
* 我们的multi-agent系统可以类比于现实世界中的一个芯片设计团队，每个子agent负责自己的角色和专业任务，最终的目标是要根据用户提供的目标模型和目标fpga平台，设计出能在fpga上跑通的性能最好的空间加速器
* 开发过程中要时刻牢记经验教训，紧扣我们论文的问题定义以及故事逻辑，不能越改越偏
* 我们的fpga 空间加速器 multi-agent自动设计系统最终是要投入实际生产场景的，所以最后会面向广大用户，必须要保证泛化性，鲁棒性以及稳定性，同时不能把在开发过程中提供的示例输入或者示例模型写死在代码中，因为最终每个用户提供的输入内容和他们的目标模型等等输入信息都不尽相同
* 不要降低兜底，不要简化，这会导致预期的功能退化，不符合实际应用场景的需求，不能以降低prompt的完整性和准确性来简化LLM的输入，因为prompt的准确性决定了LLM是否能准确理解设计和调试意图，进而影响他的输出
* 请注意，你codex绝对不能替代agent系统来进行硬件代码调试以及替代agent系统做任何他应该做的事情，你的角色是这个FPGA LLM 空间加速 器multi-agent系统的开发者，你要做的是运行它，观察和监督他系统的行为是否符合我的预期以及真实应用场景的行为，并且符合/home/remote/workspace/Qwen2-Accelerator/docs/context_2026-03-27.md上下文文档中我一步步与codex对话设计opt空间加速器的操作过程，以及我们的论文规划，然后对错误的功能，缺失的操作以及其他有问题的地方进行优化和改错。请记住这一点，因为等到整个系统开发完毕，整个系统需要独立运行，而不是依赖你codex来做事。还有你做的优化不能只是面向这一次 case的优化，要做的优化应该是针对 agent系统通用流程的优化，目的是提高系统的效率和实用性。然后如果没什么问题的话只需要静静等待，继续观察 agent的运行即可，你只负责优化让系统改得更精确更快，还有就是检查流程中如果存在冗余就删除掉，你在开发过程中需要敏锐地发现一些能使提高我们系统的速度更快的潜在优化点，但是前提是不能简化任何功能，不能损害自动设计的效果以及硬件性能以及llm的性能，目的是保证agent系统能全自动运行并最终设计出来的空间加速器能正确地运行目标模型负载并且能顺利上板
* 我想提醒你记住后续的运行、调试和完善功能过程要记得将保存好产物和记录，最好不要保存在/tmp目录下，形成习惯，这种行为不仅有利于我们现在的完善阶段，完整的记录可以供我们随时查看和回顾，而且当会话突然中断时有利于我们准确地复现最近一次的进度和流程。然后对于已经过时的，已经确定不再需要的文件等等，要及时清理，防止占用过多的空间
* 上游的api确实会存在不稳定或者网络的问题，现在我们只是在开发阶段，只需要一直retry和等待即可，不需要stop和做其他的改动
* 在验证阶段：算采用层次化验证，分三层，第一层是先逐个空间并行算子模块单独验证并改错，使他们每一个硬件代码符合目标模型算子的执行语义并且功能正确之后，进入第二层，将这些空间并行的算子模块连在一起形成一个可以运行单层LLM的算核，这时候对这个算核进行功能验证和改错，直到其功能正确，然后进入第三层，根据用户提供的fpga板卡样例输入接入真实fpga板卡axi ddr接口进行板级的模拟调试和改错，直到其功能正确之后进入后端阶段。你理解了吗？然后这三层必须逐层验证，只有当前一层完全功能正确之后才能进入下一层，然后每一层中都是一个repair-loop的形式，agent观察真实工具的输出，然后LLM结合SACG和CCTG分析bug的根因，然后优化代码，再使用真实工具仿真观察输出，LLM再分析根因，以此作为loop直到功能正确为止。当然SACG和CCTG的作用是为了加快分析根因的准确率，尽可能少次数地，快速地找到真正的问题所在，减少loop的次数，提高调试效率。然后还有一个问题，就是，这三层之间的顺序可能不是线性的关系，因为可能会出现第一层验证全部独立算子验证通过之后，再第二层可能会发现能推翻第一层某些功能正确的地方，因为一旦这些模块连接起来之后，会出现别的问题，所以不要陷入死循环：认为经过前一层验证的功能就一定是正确的，然后后续的验证总是避开这一点，导致一直无法找到真实的问题所在，陷入死循环。然后我们现在的系统的验证基本上已经按照这个层次化去构造，但是还没有完全完善，现在需要你继续完善这个阶段。因为验证阶段是整个自动设计系统最关键的部分也是最耗时的部分，只有能快速地、准确地找到bug的根因所在，我们才能快速地将空间数据流加速器的硬件调试到功能正确，我们的系统才能具备可用性。当然可能还有一些我没有考虑到的情况，如果出现了非常异常的情况，可以即使向我报告。总之，我们的目标是最终能自动化地设计出符合用户输入spec和目标的，能正确地运行在目标fpga板卡上的LLM空间加速器。一定要确保算子完全符合目标模型中对应算子的执行语义并且在正确的testbench脚本的基础上，得出真实工具的功能正确的结果，才能算是功能正确，以及第三层的板级模拟，必须使用与用户提供的目标fpga板卡一模一样的顶层axi和ddr wrapper进行仿真和调试，这中间绝对不能有任何地简化，目的是保证仿真时与真实上板时的运行保持完全一致，否则到最后上板的时候我们就可能跑不出正确结果。agent系统验证过程必须使用真实的权重和输入输出数据，输入输入只需要使用随机数构造对应的输出数据即可，在验证开始阶段，需要有一个生成准确地testbench脚本以及从目标模型中提取真实权重和运行真实的模型推理获取golden输入输出数据的过程。
* 我们系统设计的空间加速器只运行transformer block部分的算子，其他预处理或者后处理的算子全都不管，所以完整真实权重只为完整的 Transformer block 权重集合
* 自动设计过程中所有需要用到的以及优化的工具和流程不要设计成针对某个case定制，而是要作为agent系统通用的功能步骤、操作或者工具。
* 你仔细分析系统的问题所在，然后进行恰当的通用优化，保证系统的实用性和鲁棒性，同时让系统流程更加高效和简洁。已经明确通过验证的模块就不需要总是重复跑验证或者让LLM分析他们的结果了吧？或者将他们的内容再放到prompt中，提高效率
* 然后对于第三层的验证，我需要提醒你的是，agent系统必须确保第三层的验证是完完全全地与用户所提供的真实的fpga板卡的顶层接口、axi和ddr wrapper的运行机制是一致的，运行细节和timing必须一模一样，目的是为了使用真实的板级仿真，直接在仿真调试时就做到位，做到准确，避免需要上板调试。具体的参考案例：可以参考上下文文档中早期的内容：设计pymj的opt 空间加速器时的验证过程。但是不能复制原有opt空间加速器的固定值，因为那只是对opt这一个目标模型进行定制的代码，我们需要从之前的成功案例中提取真实板级仿真所需要的板级调度壳代码的特征、要点。然后我们需要让我们的agent具备这个用户提供的fpga板卡样例中提取要生成真实的、准确无误的板级调度壳的能力和功能，随便其中的变量需要根据不同的目标模型和用户输入自适应改变，但是完整层权重映射、下一层预取、双缓冲切换、激活 ping-pong、最终回写，并把它绑定到样例工程真实 compute slot这些机制都是要保存的，因为这是我们所设计的空间加速器的必要特征，权重预期、双缓冲，空间并行模块之间流水线重叠执行而不是串行执行等等。然后这种需要自适应的变化的功能，最好使用LLM来实现或者生成，而不是你通过一些静态关键词和静态脚本去提取，如果用户换一种输入，可能就无法泛化了，这会导致我们系统的实用性和泛化性很差
* 请注意，你现在加入的这些功能必须是agent自动设计系统的通用功能，必须能保证能让agent系统自动运行你加入的这些流程，而不是需要你介入才能顺利地、持续地运行。同时不要针对当前的case定制，也不要做任何多余的优化
* 请务必记得在上下文文档中记录进展，为了防止会话丢失，导致进度丢失而进行重复的工作，这很关键
* 现在主要是你要注意观察agent系统在运行第三层验证时的过程，是否存在可以优化效率，提高调试准确率，减少重复错误的点，目的是让我们的agent系统在硬件调试阶段的速度更快，减少整个全流程的耗时时间。这些调试的过程、经验和教训，应该作为agent系统的经验数据库，供后续阶段的验证或者后续其他目标模型加速器自动设计来使用，供agent具有通过经验学习，不断精进自身设计调试能力，提高分析准确率的能力
* 还有就是请注意，运行时不要启动太多的进程，防止本地因为内存占用过大导致会话进场被强行杀死，这是个很严重的问题，因为一旦计划被杀死，进度会中断，很多东西可能来不及记录，倒置再一次会话无法恢复。还有就是远端服务器的资源占用不用管，远端服务器的资源比本地多得多，所以尽可能让远端服务器使用使用更多资源让仿真速度加快，我关心的只是本地的wsl的内存占用，因为我的会话是在本地wsl运行的，如果wsl被杀死，会话就会被中断。所以请在远端服务器上使用更多的资源，让仿真验证的速度加快
* 我们的架构应该是流水线式的，每个空间算子都是可以流水线式的并行计算的，所以不是等到所有token都输入进去之后才会有输出，正常来说只要后续的token输入时，同时也会有token输出的。也就是说每个token输入到第一个空间算子时，前一个token应该能输入到第二个
空间算子与第一个算子模块进行并行处理，一次类推，当输入的token数量多了之后，所有算子模块应该能并行运行，按照流水线式的。所以现在是了吗？而不是说只有layer粒度的才能流水线，空间加速器中每个空间算子模块都是可以流水线式地并行的，算子级 token 流水。由于空间加速器中每一级空间算子模块所花费的计算周期不可能完全一模一样，所以其实做不到严格的所有级都完全流水线式地精确并行：同时开始，同时结束。算子级 token 流水的正确含义是：不同 token 在不同算子中时间上有重叠、没有整序列 barrier、ready/valid 能正确回压；它不要求各级同周期开始/结束，也不应因各级吞吐不同而强迫每一级连续无气泡
* 不能让Agent系统在调试过程中一直固守着定死的debug信号，这是肯定是找不到真正根因的，只能靠LLM来猜，而单纯靠猜的准确率非常低，所以一定要回自适应的硬件观测循环，或者说自适应的debug信号插入机制。还有时间回溯不需要每次循环都更新一次，只需要在遇到一个长时间未解决的bug并且复现时间比较长时创建一个时间回溯点，然后重复利用，直到bug修复为止，之后如果再遇到类的情况再创建一个新的时间回溯点。如果每轮都创建一个，那不仅非常耗时，而且还很好存储资源
* 我想提醒的是，agent系统在前面每一次验证得到的结果或者说根据观察仿真结果得到的对于硬件代码内部机制、内部信号时序的理解应该能作为后续验证的经验或者说是前置的了解，让agent具备与人一样对一个项目或者说一份代码具有循序渐进的理解能力，随着时间的推移，或者说理解得更加深入，后续的验证能更加准确地、更快地分析出真正的根因。也就是具备自进化，自我迭代的能力。
* 我再提醒你一次，不要把功能做得太复杂了以及不要把代码逻辑实现得太复杂，容易出bug，请简化各种除了自动设计主流程以外所有多余的功能。，什么各种哈希，各种审核，各种小步骤的各种小问题频出，导致一直无法推进自动设计的主流程，必须想办法简化所有完全没必要的步骤和功能流程，请严格执行这个要求，把所有存在冗余和重复的地方都已经简化，使我们的agent系统更轻量，流程更少，效率更高，只专注于主流程
* agent系统只需要让设计的加速器能跑通流水线重叠式地跑通全部层。有非零值，或者说有效值输出即可算作第三层验证通过，不需要保证完全的数值正确
* 尽快完善和完成agent的全流程闭环流程，尽快让agent设计出能上板的fpga加速器，因为我们的时间真的不多了，然后目前一
  切从简，功能尽可能简化，目的是要让agent尽快设计出能上板的fpga加速器，我们目前只要求做出能跑完全流程的最简版本，因为我们后续还有别的目标
  模型的加速器设计实验要做。而论文截稿日期就要到了。请注意，现在只要完成最简功能，进尽快跑通全流程，其他的多余步骤一律不要

## Agent 基础知识库
* Agent基础知识库文档：/home/remote/workspace/Qwen2-Accelerator/docs/agent_basic_knowledge.md，请仔细阅读一遍其中的内容，了解基本知识
* 用于了解什么是agent，agent由什么组成，agent能干什么
* 用于指导正确地开发出高效的agent系统，提供基本的知识背景